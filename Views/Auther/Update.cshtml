@model BookStore.Models.Domain.Auther
@{
}

<div class="container pt-5">
	<div class="row">
		<div class="col-md-6">
			<h2>Update Author</h2>
			<form asp-action="Update" method="post">
				<input type="hidden" asp-for="Id" />
				<div class="form-group">
					<label asp-for="AutherName">Auther Name *</label>
					<input type="text" class="form-control" asp-for="AutherName" />
					<span asp-validation-for="AutherName" class="text-danger"></span>
				</div>
				@if (TempData["msg"] != null)
				{
					<div class="form-group">
						@TempData["msg"]
					</div>
				}
				<div class="form-group pt-2">
					<button type="submit" class="btn btn-dark">Save</button>
					<a herf="/Auther/GetAll" class="btn btn-primary">List</a>
				</div>
			</form>
		</div>
	</div>
</div>